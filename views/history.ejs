<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login History</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <style>
      body {
        font-family: "Ubuntu", sans-serif;
        background-color: #e0e4e8;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        color: #333;
      }
      .container {
        display: flex;
        flex-direction: column;
        width: 90%;
        max-width: 1200px;
        margin: 20px;
        background-color: #ffffff;
        border-radius: 16px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        padding: 20px;
      }
      .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
      }
      .header h1 {
        color: #2c3e50;
        margin: 0;
        font-size: 36px;
        font-weight: 700;
      }
      .header a {
        color: #3498db;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease, transform 0.3s ease;
        border: 2px solid transparent;
        padding: 8px 16px;
        border-radius: 8px;
        background: rgba(52, 152, 219, 0.1);
        box-shadow: 0 2px 4px rgba(52, 152, 219, 0.2);
        display: flex;
        align-items: center;
      }
      .header a:hover {
        color: #2980b9;
        text-decoration: underline;
        transform: scale(1.05);
        background: rgba(52, 152, 219, 0.2);
        box-shadow: 0 4px 6px rgba(52, 152, 219, 0.3);
      }
      .header a i {
        margin-right: 8px;
      }
      .login-cards {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 20px;
      }
      .login-card {
        background: #ffffff;
        border-radius: 12px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        padding: 20px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;
        transition: box-shadow 0.3s ease, transform 0.3s ease;
        position: relative;
        overflow: hidden;
        border: 2px solid #f0f4f8;
      }
      .login-card:hover {
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.2);
        transform: translateY(-5px);
      }
      .login-card span {
        font-size: 16px;
        color: #34495e;
      }
      .login-card .date {
        font-weight: 700;
        color: #2980b9;
        margin-bottom: 10px;
      }
      .login-card .time {
        color: #7f8c8d;
      }
      .login-card .btn {
        margin-top: 10px;
        padding: 10px 20px;
        border: none;
        border-radius: 8px;
        font-size: 14px;
        font-weight: 500;
        color: #fff;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.3s ease;
        display: flex;
        align-items: center;
      }
      .login-card .btn i {
        margin-right: 8px;
      }
      .btn-color-1 {
        background-color: #e74c3c;
      }
      .btn-color-2 {
        background-color: #1abc9c;
      }
      .btn-color-3 {
        background-color: #f39c12;
      }
      .btn-color-4 {
        background-color: #3498db;
      }
      .btn-color-5 {
        background-color: #95a5a6;
      }
      .btn-color-1:hover {
        background-color: #c0392b;
      }
      .btn-color-2:hover {
        background-color: #16a085;
      }
      .btn-color-3:hover {
        background-color: #e67e22;
      }
      .btn-color-4:hover {
        background-color: #2980b9;
      }
      .btn-color-5:hover {
        background-color: #7f8c8d;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1><i class="fas fa-history"></i> Login History</h1>
        <a href="/dashboard"
          ><i class="fas fa-tachometer-alt"></i> Back to Dashboard</a
        >
      </div>
      <div class="login-cards">
        <% loginHistory.forEach((login, index) => { %>
        <div
          class="login-card"
          style="border-color: <%= index % 5 === 0 ? '#e74c3c' : index % 5 === 1 ? '#1abc9c' : index % 5 === 2 ? '#f39c12' : index % 5 === 3 ? '#3498db' : '#95a5a6' %>;"
        >
          <span class="date"
            ><%= new Date(login.timestamp).toLocaleDateString() %></span
          >
          <span class="time"
            ><%= new Date(login.timestamp).toLocaleTimeString() %></span
          >
          <button class="btn btn-color-<%= index % 5 + 1 %>">
            <i class="fas fa-calendar-day"></i> Check Previous Day Activity
          </button>
        </div>
        <% }); %>
      </div>
    </div>
  </body>
</html>
